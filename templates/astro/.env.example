# Local build-time URL used for canonical links, robots.txt, and the sitemap.
# Runtime variables and secrets must be configured with `smallforce app var`
# and `smallforce app secret`; do not put server secrets in this file.
PUBLIC_SITE_URL=http://localhost:4321

# Do not put SMALLFORCE_API_KEY or Cloudflare secrets in generated app code.
